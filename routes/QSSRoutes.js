const express=require('express');
const QSS=require('../models/QSSModel');
const QSSParam=require('../models/QSSParam');
const QSSvalues=require('../models/QSSvalueModel');
const router=express.Router();

router.get('/QSS', (req,res)=>{});

module.exports = router;
