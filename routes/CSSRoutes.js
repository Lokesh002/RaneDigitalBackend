const express=require('express');
const CSS=require('../models/CSSModel');
const CSSParam=require('../models/CSSParam');
const CSSvalues=require('../models/CSSvalueModel');

const router=express.Router();

router.post('/', (req,res)=>{
    
});

module.exports = router;
